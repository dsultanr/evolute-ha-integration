# Интеграция Evolute для Home Assistant

[![hacs_badge](https://img.shields.io/badge/HACS-Custom-41BDF5.svg)](https://github.com/hacs/integration)

Интеграция Home Assistant для автомобилей Evolute (i-Joy, i-Pro, i-Sky, i-Space и т.д.) через облачный сервис app.evassist.ru.

<img width="1073" height="1653" alt="image" src="https://github.com/user-attachments/assets/bacb9294-98a1-44e8-bc8f-9967ff0c5f67" />

## Возможности

- **50+ сенсоров**, включая:
  - Заряд тяговой батареи и запас хода
  - Напряжение 12В АКБ, температуры (батарея, охлаждающая жидкость, салон, климат, снаружи)
  - Одометр, обороты вентилятора климат-контроля
  - GPS-локация (широта, долгота, курс), уровень GSM-сигнала
  - График ТО: сколько дней и километров осталось, дата и пробег следующего обслуживания
- **Бинарные сенсоры** для дверей, багажника, фар, зажигания, центрального замка, обогрева и охлаждения салона, статуса "онлайн", движения, зарядного пистолета, доступного обновления прошивки и расхождения версии прошивки с ожидаемой
- **Кнопки управления**: центральный замок, обогрев, охлаждение, багажник, звуковой сигнал/поиск, старт/отмена подготовки к поездке
- **Карточка кнопки с удержанием** (`custom:evolute-hold-button`) — защита от случайного касания, индикация отправленной команды и блокировка недоступных команд
- **Трекер местоположения** на карте Home Assistant
- Поддержка нескольких автомобилей на одном аккаунте

## Авторизация: почему не логин/пароль

Официальное приложение Evolute входит по номеру телефона и SMS-коду, но перед отправкой кода сайт требует пройти Yandex SmartCaptcha. Автоматизировать это программно ненадёжно (или является обходом защиты, чего эта интеграция сознательно не делает).

Вместо этого используется тот же подход, что и в проекте [evolute_proxy](https://github.com/hackruu/evolute_proxy) и Android-приложении [i-joy](https://github.com/wzd2002/i-joy): вы **один раз** входите в app.evassist.ru через браузер и копируете два cookie-токена. Дальше интеграция сама обновляет `access_token` через `refresh_token` (он живёт около 90 дней) и сохраняет обновлённые значения, так что повторно вставлять токены обычно не требуется — разве что `refresh_token` будет отозван сервером.

### Как получить токены

1. Откройте https://app.evassist.ru в браузере и войдите в свой аккаунт (телефон + SMS-код)
2. Откройте инструменты разработчика (F12) → вкладка **Application** (Chrome) или **Storage** (Firefox)
3. Слева выберите **Cookies** → `https://app.evassist.ru`
4. Найдите и скопируйте значения двух cookies:
   - `evy-platform-access`
   - `evy-platform-refresh`
5. Вставьте их в форму настройки интеграции в Home Assistant

## Установка

### HACS (Рекомендуется)

1. Откройте HACS в вашем Home Assistant
2. Перейдите в раздел "Интеграции"
3. Нажмите три точки в правом верхнем углу
4. Выберите "Пользовательские репозитории"
5. Добавьте URL репозитория: `https://github.com/dsultanr/evolute-ha-integration`
6. Категория: "Интеграция"
7. Нажмите "Добавить"
8. Найдите "Evolute"
9. Нажмите "Загрузить"
10. Перезапустите Home Assistant

### Ручная установка

1. Скопируйте директорию `custom_components/evolute` в директорию `custom_components` вашего Home Assistant
2. Перезапустите Home Assistant

## Настройка

1. Перейдите в **Настройки** → **Устройства и службы**
2. Нажмите **"+ ДОБАВИТЬ ИНТЕГРАЦИЮ"**
3. Найдите **"Evolute"**
4. Вставьте **Access Token** и **Refresh Token** (см. раздел выше)
5. Нажмите **Отправить**

Все автомобили вашего аккаунта появятся автоматически со всеми доступными сенсорами.

## Сенсоры

### Обычные сенсоры

**Батарея и запас хода:**
- Заряд тяговой батареи (%)
- Запас хода (км)
- Уровень топлива (%) и запас хода по топливу (км) — поле присутствует в API даже для электромобилей
- Напряжение 12В АКБ

**Температуры:**
- Температура батареи
- Температура охлаждающей жидкости
- Температура в салоне
- Температура снаружи
- Текущая и целевая температура климат-контроля
- Скорость вентилятора климат-контроля

**Движение и GPS:**
- Одометр
- Широта, долгота, курс

> Скорость, высота, количество спутников и HDOP сервер больше не отдаёт (см. «Переход на carV2»).
> Сущности высоты, спутников и HDOP остались, но постоянно показывают `unknown`; сенсор скорости удалён.

**Обслуживание (опрашивается раз в 15 минут):**
- До ТО (дней) и до ТО (км)
- Дата следующего ТО и пробег, к которому оно назначено
- Статус обслуживания

**Прочее:**
- Время последнего выхода на связь
- Время снятия телеметрии и время последнего определения координат
- Статус автомобиля текстом (как в приложении: «Доступен», «В пути», «Зажигание включено», «Идет зарядка», «Не в сети»)
- Состояние соединения (`connected` / `disconnected`)
- Уровень GSM-сигнала
- Версия прошивки телематического блока и версия, которую ожидает сервер
- Количество предупреждений
- Подготовка к поездке: осталось времени, время старта, заданная длительность
- Время последнего запроса телеметрии и последней отправленной команды

### Бинарные сенсоры

- Зажигание
- Центральный замок
- Двери (водительская, пассажирская, задние левая/правая)
- Багажник
- Фары
- Климат-контроль включён
- Обогрев лобового стекла
- Онлайн (связь с автомобилем)
- Движение (автомобиль не на стоянке)
- Идёт подготовка к поездке (прогрев/охлаждение по таймеру)
- Обогрев салона включён
- Охлаждение салона включено
- Подготовка к поездке поддерживается автомобилем / сейчас недоступна / завершилась ошибкой
- Зарядный пистолет вставлен
- Идёт зарядка
- Телеметрия загружена (сервер успел собрать состояние автомобиля)
- Версия прошивки не совпадает с ожидаемой
- Настройки телематического блока не совпадают с ожидаемыми

> Сенсоры «Климат-контроль включён» и «Обогрев лобового стекла» остались, но сервер
> больше не присылает эти поля, поэтому они постоянно показывают `unknown`.

  Первые два флага не противоположны друг другу: «поддерживается» означает, что функция у автомобиля вообще есть, «сейчас недоступна» — что запустить её прямо в этот момент нельзя. Поэтому оба могут быть включены одновременно.
- Есть предупреждения
- Доступно обновление прошивки
- Передача местоположения включена

Обогрев и охлаждение — единственные состояния, которых нет в `sensorsData`: сервер отдаёт их только в описании кнопок приложения (`buttons.main[].state`), оттуда интеграция их и берёт.

## Кнопки управления

- **Переключить замок** — центральный замок (открыть/закрыть)
- **Переключить обогрев** — включить/выключить обогрев салона
- **Переключить охлаждение** — включить/выключить кондиционер
- **Переключить багажник** — открыть/закрыть багажник
- **Сигнал и поиск** — короткий гудок и мигание аварийкой
- **Старт подготовки к поездке** — запустить сценарий прогрева/охлаждения по таймеру
- **Отмена подготовки к поездке** — не подтверждено в перехваченном трафике, добавлено по аналогии; если не сработает — сообщите в Issues

Важно: у API Evolute нет отдельных команд "включить"/"выключить" для замка, обогрева, охлаждения и багажника — есть только команда-переключатель (toggle). Кнопки просто вызывают её, поэтому текущее состояние стоит проверять по соответствующему бинарному сенсору перед нажатием.

### Когда команды недоступны

Сервер отказывает в командах не только флагом «кнопка выключена». Приложение
дополнительно гасит управление, и интеграция повторяет те же правила — кнопка
становится `unavailable`, а причина попадает в её атрибут `blocked_reason`:

| Условие | Что блокируется |
| --- | --- |
| Автомобиль не в режиме паркинга | всё |
| Автомобиль оффлайн | всё |
| Центральный замок открыт | всё, кроме замка и сигнала/поиска |

Кнопки подготовки к поездке приложение показывает только аккаунтам с правом
`canViewTaggedCarButtons` (или администраторам). Если у аккаунта его нет,
интеграция тоже их не предлагает, а сенсоры подготовки не заполняются.

### Обратная связь по командам

Автомобиль подтверждает команду телеметрией не сразу — обычно через 5–30 секунд. Чтобы состояние не «висело» до следующего планового опроса:

- после отправки команды интеграция дополнительно опрашивает сервер через 2, 5, 9, 14, 20, 30 и 45 секунд;
- пока подтверждения нет, кнопка помечена атрибутом `pending: true` и меняет иконку на песочные часы;
- как только соответствующий бинарный сенсор изменился, `pending` снимается автоматически;
- команды без наблюдаемого состояния (сигнал) `pending` не получают вовсе: ждать нечего, телеметрия от них не меняется, и опрашивать сервер незачем;
- если сервер сообщает, что команда сейчас не принимается (`buttons.main[].disabled`), кнопка становится `unavailable`, а её атрибут `blocked` — `true`. Например, при включённом зажигании Evolute не принимает команды обогрева, охлаждения и багажника — соответствующие кнопки гаснут.

Подготовка к поездке — единственная пара команд, которой нет в `buttons.main[]`, поэтому её доступность берётся из `preparation_script`:

- **Старт подготовки** заблокирован, если сценарий уже выполняется, помечен `disabled` или недоступен для этого автомобиля;
- **Отмена подготовки** заблокирована, только когда отменять нечего — то есть сценарий не выполняется. Запущенный сценарий остаётся отменяемым в любом случае, даже если сервер помечает подготовку как `disabled`, иначе его нечем было бы остановить.

### Карточка кнопки с удержанием

Обычная `type: button` отправляет команду от случайного касания — для открытия замка или багажника это неприятно. В комплекте идёт карточка `custom:evolute-hold-button`:

- команда уходит **только после удержания** (по умолчанию 700 мс); кольцо вокруг иконки заполняется всё время удержания, показывая, сколько осталось держать;
- при коротком касании карточка вздрагивает, даёт вибро-отклик и всплывающую подсказку «удерживайте ~0.8 с для подтверждения» — команда при этом не отправляется;
- кнопка заблокирована только на время самого запроса — это доли секунды, показывается спиннером;
- как только сервер принял команду, на секунду загорается зелёная галочка «отправлено», и кнопка снова рабочая;
- если машина ещё не подтвердила изменение телеметрией, это видно по спокойной пульсации кольца и подписи «ждём машину» — но нажимать при этом можно: защитой от случайного касания служит само удержание, а не блокировка;
- недоступная команда отображается затенённой, касание объясняет причину.

Файл копируется в `/config/www/evolute/evolute-hold-button.js` и регистрируется как ресурс Lovelace автоматически. Для дашбордов в YAML-режиме ресурс нужно добавить вручную:

```yaml
resources:
  - url: /local/evolute/evolute-hold-button.js
    type: module
```

Пример использования:

```yaml
type: custom:evolute-hold-button
name: Замок
icon: mdi:lock-outline
entity: button.evolute_CAR_ID_HERE_central_lock_toggle
state_entity: binary_sensor.evolute_CAR_ID_HERE_central_lock
state_labels:
  "on": Открыт
  "off": Закрыт
hold_time: 800        # мс удержания, по умолчанию 700
require_hold: true    # false — обычное касание (для безобидных команд вроде сигнала)
```

## Визуализация автомобиля

Интеграция включает готовую **схему автомобиля сверху** (переиспользована из [prizrak-ha-integration](https://github.com/dsultanr/prizrak-ha-integration) — расположение дверей совпадает):

- SVG-файлы автоматически копируются в `/config/www/evolute/` при запуске интеграции
- 🔵 **Синий контур**: центральный замок закрыт
- 🟡 **Жёлтая/оранжевая подсветка**: конкретная дверь или багажник открыты
- 🔧 **Иконка двигателя**: зажигание включено
- ❄️ **Иконка климата**: обогрев/охлаждение активны

Капот на схеме не подсвечивается — API Evolute не отдаёт его статус.

### Пример карточки Dashboard

Готовый пример: [examples/car-card.yaml](examples/car-card.yaml). Замените `CAR_ID_HERE` на ID вашего автомобиля (виден в entity_id любого сенсора, например `sensor.evolute_68f2.._battery_percentage`).

Кнопки управления в примере используют встроенную карточку `custom:evolute-hold-button` (см. выше). Сторонние HACS-карточки `card-mod`/`button-card` не нужны.

## Примеры автоматизаций

### Уведомление о низком заряде батареи

```yaml
automation:
  - alias: "Evolute: Низкий заряд батареи"
    trigger:
      - platform: numeric_state
        entity_id: sensor.evolute_CAR_ID_HERE_battery_percentage
        below: 20
    action:
      - service: notify.mobile_app
        data:
          title: "Низкий заряд автомобиля"
          message: "Заряд батареи Evolute: {{ states('sensor.evolute_CAR_ID_HERE_battery_percentage') }}%"
```

### Уведомление об открытой двери

```yaml
automation:
  - alias: "Evolute: Открыта водительская дверь"
    trigger:
      - platform: state
        entity_id: binary_sensor.evolute_CAR_ID_HERE_door_fl
        to: "on"
    action:
      - service: notify.mobile_app
        data:
          title: "Оповещение о машине"
          message: "Водительская дверь была открыта!"
```

### Автоматический прогрев по расписанию

```yaml
automation:
  - alias: "Evolute: Прогрев перед работой"
    trigger:
      - platform: time
        at: "07:30:00"
    condition:
      - condition: state
        entity_id: binary_sensor.evolute_CAR_ID_HERE_online
        state: "on"
    action:
      - service: button.press
        target:
          entity_id: button.evolute_CAR_ID_HERE_prepare_start
```

## Переход на carV2

Осенью 2026 года сервис перевёл телеметрию на новый эндпоинт
(`/client-bff-service/telemetry/{imei}` вместо `/car-service/tbox/{car_id}/info`).
Старый продолжает отвечать, но данными его больше не наполняют: он отдаёт снимок
суточной давности. Интеграция, начиная с 1.4.0, читает новый.

Что при этом изменилось:

- **Пропали поля.** Скорость, высота, количество спутников, HDOP, текущая температура
  климат-контроля, состояние климата и обогрева лобового стекла в новом ответе
  отсутствуют. Взять их неоткуда — их не показывает и само приложение.
- **Появились поля.** Уровень GSM-сигнала, зарядный пистолет, готовность телеметрии,
  сверка версии прошивки и настроек с ожидаемыми.
- **Статус автомобиля считается на клиенте.** Новый ответ не содержит готовой
  подписи статуса, приложение выводит её из той же телеметрии. Интеграция повторяет
  эту логику, поэтому сенсор статуса показывает то же, что и приложение.
- **Команды шлются иначе** — `POST /client-bff-service/telemetry-commands` с именем
  команды и IMEI, в ответ приходит идентификатор, по которому опрашивается результат
  (`pending` → `delivered` → `success` / `error`).

## Устранение неполадок

### Интеграция просит повторную авторизацию (reauth)

Это означает, что `refresh_token` был отозван сервером (например, после смены пароля или выхода из всех сессий). Повторите шаги получения токенов из браузера и введите новые значения в открывшейся форме — переустанавливать интеграцию не нужно.

### Интеграция не загружается

1. Проверьте логи Home Assistant: **Настройки** → **Система** → **Логи**
2. Ищите ошибки, связанные с "evolute"
3. Убедитесь, что скопированные токены не содержат лишних пробелов

### Сенсоры показывают "недоступен"

1. Проверьте интернет-соединение
2. Убедитесь, что app.evassist.ru доступен и автомобиль виден в приложении
3. Проверьте логи Home Assistant на наличие ошибок HTTP-запросов

## Поддержка

- **Проблемы**: [GitHub Issues](https://github.com/dsultanr/evolute-ha-integration/issues)

## Лицензия

Этот проект лицензирован под лицензией MIT — см. файл [LICENSE](LICENSE) для подробностей.

## Благодарности

Реверс-инжиниринг API опирался на публичные наработки проектов [evolute_proxy](https://github.com/hackruu/evolute_proxy) и [i-joy](https://github.com/wzd2002/i-joy).

[Поблагодарить](https://pay.cloudtips.ru/p/aa2fce54)

Создано [@dsultanr](https://github.com/dsultanr)

## Отказ от ответственности

Это неофициальная интеграция и не связана с компанией Evolute или сервисом app.evassist.ru. API не документирован публично и может измениться в любой момент.
